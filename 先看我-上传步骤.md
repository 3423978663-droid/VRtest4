# 先看我（已修复扫码上传）

本次修复：云函数在 Lambda 兼容模式下需调用 connectLambda 才能使用 Netlify Blobs。

## 上传到 GitHub
1. 双击解压。
2. `Cmd + A` 全选。
3. GitHub 仓库 → Add file → Upload files → 拖入 → Commit changes。

## Netlify 自动更新
1. 打开 app.netlify.com 进入网站，等 1～2 分钟自动重新部署。
2. 电脑按 `Cmd + Shift + R` 强制刷新。
3. 再测试手机扫码上传。

> 不要上传 .zip 本身；必须包含根目录 package.json。
