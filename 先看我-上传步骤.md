# 先看我（最新版）

本次更新：
- 去掉“自动斜正”，避免把图转歪；请尽量拍正上传
- 上传速度优化：确认照片时压缩到 1600px、画质 0.85；手机端压缩到 1800px、0.8
- 保留上传后的“确认照片”步骤：可手动旋转、框选裁剪
- 词库约 12800 词（中考/高考/雅思/专四/专八）
- 取消联网查词，词库外手动记录
- PDF 两栏 + 右上角时间

## 上传到 GitHub
1. 双击解压，`Cmd + A` 全选。
2. GitHub 仓库 → Add file → Upload files → 拖入 → Commit changes。

## Netlify 自动更新
1. 打开 app.netlify.com，等 1～2 分钟自动部署。
2. 电脑按 `Cmd + Shift + R` 强制刷新。

> 不要上传 .zip 本身；必须包含根目录 package.json。
